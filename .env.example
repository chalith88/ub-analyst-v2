# Environment Variables for UB Scraper

# Node environment (development | production)
NODE_ENV=production

# Server port
PORT=3000

# Playwright configuration (set to 1 in Docker/production to skip browser download)
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=0

# Railway deployment (CI/CD only)
# RAILWAY_TOKEN=your-railway-token-here
